let pi=Math.PI;

//arrow function
let sum=()=>{
    return 2+3;
}

let maxNum=Math.max(2,6,8,1,20);

let obj={
    name:"sanmati",
    
}

module.exports={pi,sum,maxNum,obj};//exporting value to app.js